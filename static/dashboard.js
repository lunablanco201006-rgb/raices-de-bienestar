let selected=null,chart=null;document.querySelectorAll(".moods button").forEach(b=>b.onclick=()=>{document.querySelectorAll(".moods button").forEach(x=>x.classList.remove("selected"));b.classList.add("selected");selected={mood:b.dataset.mood,score:+b.dataset.score};});async function load(){let d=await (await fetch("/api/moods")).json();let ctx=document.getElementById("chart");if(chart)chart.destroy();chart=new Chart(ctx,{type:"line",data:{labels:d.map(x=>x.created_at.slice(5,16)),datasets:[{label:"Ánimo",data:d.map(x=>x.score),tension:.35,fill:false}]},options:{scales:{y:{min:1,max:5,ticks:{stepSize:1}}}}});document.getElementById("list").innerHTML=d.slice().reverse().map(x=>`<p><b>${x.mood}</b> · ${x.created_at}${x.note?`<br><small>${safe(x.note)}</small>`:""}</p>`).join("")||"<p>No hay registros todavía.</p>"}document.getElementById("save").onclick=async()=>{let msg=document.getElementById("msg");if(!selected){msg.textContent="Selecciona una emoción.";return}let r=await fetch("/api/mood",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({...selected,note:document.getElementById("note").value})});let x=await r.json();msg.textContent=x.ok?"✓ Registro guardado.":"No se pudo guardar.";if(x.ok){document.getElementById("note").value="";selected=null;document.querySelectorAll(".moods button").forEach(b=>b.classList.remove("selected"));load()}};function safe(s){return String(s).replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[c]))}load();